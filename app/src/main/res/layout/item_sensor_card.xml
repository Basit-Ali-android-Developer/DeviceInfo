<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto">

    <data>
        <!-- Optionally, we can add a Sensor variable in the future -->
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        card_view:cardCornerRadius="16dp"
        card_view:cardBackgroundColor="?attr/cards"
        card_view:cardElevation="4dp">


        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp"
            android:layout_marginLeft="10dp">

            <TextView
                android:id="@+id/tvSensorName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Sensor Name"
                android:textSize="?attr/textSizeTitle"
                android:textStyle="bold"
                android:textColor="@color/Dashboard_text"
                android:layout_marginBottom="5dp" />

            <TextView
                android:id="@+id/tvSensorDetails"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="Details here..."
                android:textColor="@color/details"
                android:textSize="?attr/textSizeDetails" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>
</layout>
